var React = require('react-native');

var {
  Text,
} = React;

var Loading = React.createClass({

  render: function() {
    return (
      <Text>Loading</Text>
    );
  }

});

module.exports = Loading;